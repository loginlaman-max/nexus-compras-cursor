"use client";

import { FornTabelaWorkspace } from "@/components/precificacao/forn-tabela-workspace";

/** Tela única — rail de tabelas + grade adaptativa (protótipo FornTabelas). */
export function FornTabelasPageView() {
  return <FornTabelaWorkspace />;
}
